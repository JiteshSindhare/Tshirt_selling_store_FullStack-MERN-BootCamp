import React from 'react'
/**
 * NOTE: mostly same as Add product, just need to preload things about the product
 * which we want to update.
 */
export default function UpdateProduct() {
    return (
        <div>
            <h1 className="text-white">Update product.</h1>
        </div>
    );
}
